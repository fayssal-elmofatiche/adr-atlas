{
  "name": "adr-atlas",
  "private": true,
  "scripts": {
    "build": "pnpm -r build && cp -r packages/atlas-ui/dist packages/atlas-cli/dist/ui",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
